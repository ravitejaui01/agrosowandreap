# Railway uses Nixpacks by default. Root is api/ when deployed from monorepo.
[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["echo 'No build step'"]

[start]
cmd = "npm run db:init && npm start"
