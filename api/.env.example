# Copy this file to .env:  cp .env.example .env

PORT=3000

# For local PostgreSQL (Docker): run "docker-compose up -d" first
DATABASE_URL=postgres://agroforestry:agroforestry@localhost:5432/agroforestry

# On Railway, DATABASE_URL is set automatically when you add PostgreSQL.
